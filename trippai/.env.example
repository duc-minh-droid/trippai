# Backend URL for live mode
NEXT_PUBLIC_API_URL=http://localhost:8000

# Set to true for the static build that reads public/demo/*.json instead of the API
NEXT_PUBLIC_DEMO_MODE=false

# Optional: Mapbox public token for the /multi-city map
NEXT_PUBLIC_MAPBOX_TOKEN=
